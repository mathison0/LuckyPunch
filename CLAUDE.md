# LuckyPunch — 리온이 배구 봇 (2026 CSE 교류전 AI)

이 저장소는 대회 운영측 저장소(`upstream`)를 클론해 우리 작업을 얹은 것이다.
**운영측 파일은 수정하지 않는다** (읽기·분석은 자유). 우리가 추가한 것은 다음뿐이다:

- `src/code-here/LuckyPunch_v*.js` — 봇 코드 (제출물은 이 중 하나)
- `arena/` — 오프라인 대전 하네스
- `docs/strategy/` — 버전별 전략 상세 (설계 의도·측정·기각 사유). 인덱스와 `lag` 용어 정의는 `README.md`
- `CLAUDE.md` — 이 문서

```
git remote -v
  origin    https://github.com/mathison0/LuckyPunch.git          (우리 작업)
  upstream  https://github.com/SKKU-x-HYU-SW-Competition/leonyi-volleyball.git  (운영측 원본)
```

운영측이 코드를 갱신하면 `git fetch upstream && git merge upstream/main` 후
`bash arena/sync-engine.sh`로 하네스의 엔진 사본도 다시 맞춘다.

- 팀명: `LuckyPunch` / 언어: JavaScript
- 가이드: https://shaims.vercel.app/guide (팀명+비밀번호 로그인 필요)
- **제출은 사용자가 직접 한다. 에이전트가 shaims에 업로드하지 않는다.**

## 개발 루틴

```
npm start          # webpack dev server, http://localhost:8080
```

파일 저장 → 자동 재빌드("compiled successfully") → 브라우저에서 **적용(재시작)만** 다시 클릭.
서버 재시작 불필요. 예외는 F12 → Console.

개선할 때마다 `LuckyPunch_v2.js`, `v3.js` 처럼 **새 파일을 추가**해 드롭다운에서 나란히 비교한다.

### 봇 설정 UI
게임 시작 → 상단 [봇 설정] → 좌/우 각각 `키보드 | 봇 코드 | 기본 AI` 선택 → (봇이면) 파일 선택
→ [적용 (재시작)]. 상태 줄이 **"봇 코드 로드됨"** 이어야 정상.
드롭다운에 안 뜨면 파일명이 `<팀명>_v<버전>.js|py` 규약을 어긴 것.

주의: 적용하면 게임이 intro → menu 로 돌아간다. menu는 powerHit 키(Z 또는 Enter) 입력이 있어야
넘어간다. 봇/AI 슬롯은 round 진입 시에만 keyboardArray에 설치된다.

## decide 계약

```js
function decide(s) { return { x: -1|0|1, y: -1|0|1, hit: 0|1 }; }
```

- 파일 최상위에 `decide` 함수만 있으면 됨. export 불필요.
- 파일은 **매치당 딱 한 번** 실행됨 → 최상위 변수는 tick 사이에 유지된다.
- 잘못된 값 / 필드 누락 / 예외 → **그 tick만** 무입력(0,0,0), 매치는 계속.
- LEFT·RIGHT가 같은 파일을 써도 서로 다른 Worker라 전역 변수는 공유되지 않는다.

### 반환값의 파생 규칙 (자주 틀림)
- **점프 = 지상(`state===0`)에서 `y=-1`.** 별도 필드 없음. 점프 중 `y=-1`은 스매시 각도만 정함.
- **다이빙 = 지상 + `x!==0` + `hit=1`.** `hit=1`이라고 항상 파워히트가 아님.
- **파워히트 방향은 `x`가 아니라 "공이 네트 어느 쪽에 있느냐"로 결정된다.**
  `x!==0`은 단지 속도 2배. 부호는 방향과 무관.
- 파워히트 각도는 `hit=1`인 그 순간의 `y`:
  `y=-1` 아치(못박기) / `y=0` 직선 / `y=1` 하방 강스매시.
  **`y=1`은 네트에서 멀면 자기 코트에 꽂힌다(자책골).** 네트 80px 이내에서만.
- `hit=1`을 여러 tick 유지해도 중복 발동 안 됨 (state가 1→2로 바뀌어 조건이 안 맞음).

## 스냅샷

```
tick, side("LEFT"|"RIGHT")
self/opp : x, y, state, frameNumber, divingDirection
ball     : x, y, xVelocity, yVelocity, expectedLandingPointX, isPowerHit
meta     : score.self, score.opp, isPlayer2Serve, rallyFrameCount   // score는 내 관점
config   : tickFrameGroupSize   // 기본 3, 하드코딩 금지
```

- `elapsedMs = tick * 40 * config.tickFrameGroupSize`
- `expectedLandingPointX`는 **지금 이 순간의 예상**. 누가 다시 치면 확 바뀐다. 캐시 금지, 매 tick 새로 읽기.
- 자기 속도는 직전에 낸 `x` 입력이 곧 속도(수평 관성 없음). 상대 속도는 직전 스냅샷 `opp.x` 와 비교.

### self.state
| 0 정상 | 1 점프중 | 2 파워히트중 | 3 다이빙중 | 4 누움 | 5 승리포즈 | 6 패배포즈 |
조작이 먹히는 건 사실상 **0, 1** 뿐. 나머지는 애니메이션이 끝날 때까지 입력 무시.

### 좌표계 (원작 픽셀, 정규화 없음)
**y는 아래로 갈수록 증가.** 최고점일수록 y가 작다. `yVelocity > 0` = 낙하 중.

| 상수 | 값 |
|---|---|
| GROUND_WIDTH | 432 |
| GROUND_HALF_WIDTH (네트 x) | 216 |
| PLAYER_TOUCHING_GROUND_Y | 244 |
| BALL_TOUCHING_GROUND_Y | 252 |
| BALL_RADIUS | 20 |
| PLAYER_HALF_LENGTH | 32 |
| NET_PILLAR_HALF_WIDTH | 25 (기둥 = 216 ± 25) |
| NET_PILLAR_TOP_TOP_Y | 176 |

LEFT는 x ∈ (0,216), RIGHT는 x ∈ (216,432). **`side`를 보고 매번 계산할 것 — 방향 하드코딩 금지.**

## 제약

- **시간**: 목표 120ms(=tick 주기) / hard timeout 360ms(초과 시 그 tick 폐기) /
  **15 tick 연속 timeout → 워커 강제 재시작(전역 상태 전멸)**. 초기화는 60초 이내.
- **반응 지연 ~1 tick(120ms)** 은 구조적. 없앨 수 없고 상대도 동일.
  낙하지점 추적 시 데드밴드(`|dx| > 6~8`)를 두어야 진동이 안 생긴다.
- **파일 1개, 4MB 이하.** import/require 불가.
- JS 금지: `import`, `require`, `fetch`/XHR, DOM(`window`,`document`), Worker 생성.
  가능: `Math`, `Date`, `JSON`, `Array`, `Map`/`Set`, `RegExp`, `console.*`.
- 로그는 반드시 카운터로 걸러서: `if (tickCounter % 100 === 0) console.log(...)`.

## 매치 룰

- 1세트 10점 선착. 서브권은 매치 시작 시 **랜덤**.
- **한 진영 연속 5회 접촉 시 실점** (원작에 없는 규칙). 무한 토스 방지.
- 25 FPS(1프레임 40ms), 봇 결정 주기 3프레임(120ms).
- 예선: 3조 × 4팀, 1경기 1세트, 팀당 6경기. 각 조 1·2위 + 3위 중 상위 2팀 → 본선 8강.
- 본선: 3세트 2선승. 4분 경과 시 앞선 팀 승 / 동점이면 5분까지 골든볼 / 그래도 동점이면 무승부.

## 대회 당일 추가 스킬

스냅샷에 **필드가 추가만** 된다(기존 필드 불변). 새 필드를 안 봐도 매치는 돌아간다.
다만 가이드가 명시: **"수비형은 통하지 않을 가능성이 높다. 매서운 공격을 시도하라."**

## 부정행위 (고의일 때만 해당)

초기화 타임아웃 고의 유발로 상대 방해 / 물리엔진에 NaN·Infinity 주입 /
게임·상대 봇 코드 탈취·수정, 온라인 기존 봇 표절 / shaims·게임 코드 악용.

## 디버깅

- `window.__pikaVolley` 로 실시간 물리 상태 접근 가능
  (`.physics.player1/player2/ball`, `.scores`, `.state.name`, `.keyboardArray`).
- 이상하면 Minimal에 로직을 조금씩 얹어가며 이등분 탐색.

---

## 엔진 물리 (physics.js 분석 결과)

봇을 짤 때 실제로 쓰이는 수치. 전부 `src/resources/js/physics.js`에서 확인한 것.

### 파워히트 (`player.state === 2`로 공과 충돌한 순간)
```
vx = (공이 네트 왼쪽) ? +(|입력x|+1)*10 : -(|입력x|+1)*10
vy = max(|공의 vy|, 15) * 입력y * 2
```
→ `|x|=0`이면 속도 10, `|x|=1`이면 20. **부호는 방향과 무관.**
→ `y=0`이면 vy가 **정확히 0** (수평 직선), `y=1`이면 최소 30으로 하강.

### 몸통 리턴 (파워히트 없이 충돌)
```
vx = (공x - 플레이어x) / 3   (정수 나눗셈, 0이면 엔진이 -1/0/1 랜덤)
vy = -max(|공의 vy|, 15)
```

### 점프
입력 프레임에 `yVelocity = -16`, 매 프레임 `y += v` 후 `v += 1`.
정점 n=16에서 **y=108**, 총 비행 ~33프레임. state 2는 ~10프레임 지속(재입력 여유가 큼).

### 이동·경계
걷기 `x입력 * 6`px/frame, 다이빙 `8`px/frame.
플레이어 x 범위: LEFT `[32, 184]`, RIGHT `[248, 400]`.

### 공의 월드 충돌 (매 프레임 이 순서)
1. `x + vx`가 `0` 미만이거나 `432` 초과면 `vx` 반전
2. `y + vy < 0`이면 `vy = 1`
3. 네트: `|x-216| < 25 && y > 176`이면 → `y <= 192`일 땐 `vy>0`이면 반전, 아니면 `vx`를 바깥으로
4. `y + vy > 252`면 착지, 아니면 `y += vy; x += vx; vy += 1`

### 내장 AI의 약점
`expectedLandingPointXWhenPowerHit`는 네트 판정을 **일부러 틀리게** 짜 놓았다
(원본 주석: "make computer do mistakes"). 정확한 판정을 쓰면 그만큼 우위.

---

## 반드시 알아야 할 함정 — 랠리 시작 전 정지 구간

득점 후 게임은 `afterEndOfRound`(5프레임) → `beforeStartOfNextRound`(30프레임),
첫 랠리는 `startOfNewGame`(71프레임) 동안 **physics를 돌리지 않는다.**
그런데 `decide`는 그 사이에도 계속 호출된다.

얼어붙은 스냅샷을 보고 "지금 점프"라고 답하면 그 입력이 랠리 첫 프레임에 그대로
적용되어 **공이 오기 30프레임 전에 점프**해버린다. 점프는 33프레임이면 끝나므로
공이 도착할 땐 이미 착지해 있다 → 서브를 통째로 놓친다.

실제로 이것 때문에 오프라인에서 승률 100%인 봇이 브라우저에서 5-10으로 졌다.

**대응**: 공 위치가 직전 tick과 완전히 같으면 physics가 멈춘 것이다.
이때는 좌우 이동만 남기고 `y`, `hit`을 죽인다 (`LuckyPunch_v3.js`의 `decide` 참고).

## 실측값

- **반영 지연 = 1프레임.** Worker 응답은 요청을 보낸 프레임의 다음 프레임 `getInput`
  전에 도착한다 (`pendingRequestId` 전이를 브라우저에서 직접 계측).
  결정은 3프레임마다 내리고 그 입력이 3프레임 유지되므로, 봇 내부에서 가정할
  지연값은 **2**가 가장 강건하다 (지연 1/2/3 전 구간에서 승률 100/100/94%).
- **`decide` 실행시간 0.02~0.21ms.** 목표 120ms의 1/570. 여유 충분.

## 오프라인 대전 하네스

`arena/` — 브라우저 없이 실제 엔진으로 수백 세트를 돌린다. 사용법은 그 폴더의 README.
엔진이 갱신되면(당일 스킬 추가 등) `bash arena/sync-engine.sh`를 다시 돌릴 것.

## 급강하 스매시가 성립하는 조건 (직접 계측)

`y=1` 강스매시는 **네트에서 약 35px 안에서 쳐야만** 넘어간다.

공이 네트 기둥 구간(x 191~241)을 지날 때 `y <= 176`이어야 통과한다.
접촉 높이 y=84, `|x|=1`(vx=±20), vy=30으로 계산하면 y가 176을 넘기 전까지
2프레임밖에 못 가므로, 접촉 지점이 네트에서 40px 이상 멀면 네트 상단 판정에
걸린다. 더 멀면 아예 자기 코트에 꽂힌다.

RIGHT 기준 접촉 x별 착지 (접촉 높이 84, 상대 LEFT):

| 접촉 x | `y=1` 급강하 | `y=0` 평평 |
|---|---|---|
| 376 | 276 자기 코트 ✗ | 16 (19프레임) ✓ |
| 300 | 260 네트 걸림 ✗ | 60 ✓ |
| 260 | 300 ✗ | 100 ✓ |
| **240** | **140 (6프레임) ✓** | 120 ✓ |

즉 **깊은 곳에선 평평한 스매시밖에 답이 없고, 네트 앞에서만 킬샷이 나온다.**

## 시도했으나 기각한 것 — 다단 세팅

"1터치로 띄우고 2터치로 네트 앞에서 급강하" 전략을 v4에 구현해 측정했다. **순손해였다.**

이유는 물리에 있다. 몸통 세팅의 `vy = -max(|공vy|, 15)`라서 체공이 최소 30프레임,
천장(`y<0`) 판정까지 걸리면 40프레임이다. 그런데 그 시간 동안 네트 쪽으로
의미 있게 옮길 만한 `vx`(≈3 이상)를 주면 공이 그대로 네트를 넘어가거나 벽에 부딪힌다.
결국 위의 좁은 창(네트 35px)에 공을 못 놓으면서 상대에게 30~40프레임,
곧 **200px 이동거리**를 그냥 준다.

측정 (v4 vs v3, 미러 20세트): 세팅 임계값을 낮춰 자주 할수록 실측 지연 1에서
승률이 30%까지 떨어졌고, 세팅을 끄면 정확히 v3와 같아졌다.

코드는 `LuckyPunch_v4.js`에 남아 있다 (`MAX_SET_TOUCHES=1`로 켜짐).
**대회 당일 스킬로 물리가 바뀌면 다시 재볼 가치가 있다.**

## 접촉 시점 선택은 지연 환경에 대한 도박이다

v3는 "칠 수 있는 가장 이른 접촉"에 고정돼 있다. v4는 후보를 여러 개 평가해
더 좋은 시점을 고른다. 그런데 늦은 접촉을 적극적으로 고르면(`CONTACT_GAIN=30`)
실측 지연 1에서는 v3 대비 75%로 강해지지만 지연 3에서는 36%로 무너진다.
타이밍 환경에 거는 도박이므로 중립값(60)으로 뒀다.

## 버전 이력

| 버전 | 내용 | 오프라인 종합 |
|---|---|---|
| v1 | 수비 베이스라인 — `expectedLandingPointX` 추적 + 엄격한 점프. 파워히트 없음 | 21.9% |
| v2 | 공격형. 궤적 예측 + 점프 타이밍 역산 + 파워히트 6조합 시뮬레이션. **정지 구간 버그 있음** | 79.7% |
| v3 | v2 + 비행 중 요격 회피 평가 + **정지 구간 수정**. 브라우저 실전 3전 3승 | 93.1% |
| v4 | v3 + 접촉 시점 후보 비교 (세팅은 기각·OFF) | 83.3% |
| v5 | v4 + **예측 수비(maximin)** — 상대 접촉 시점의 가능 수 7종 낙하 지점을 미리 계산해 선점 | 실측 지연에서 v4 상대 100% |
| v6 | v5 + **커밋 겨냥 공격** — 상대 미래 위치 + 반응 지연 모델로 채점. v5와 동급 (맞대결 50%) | 풀 종합 92.6% |
| v7 | v6 + **공중 낙하점 버그 수정** + 시간 게이트 와리가리. **v5·v6 맞대결 60%** | 풀 종합 82.9% (v5·v6 포함) |

**실측 지연(1)에서 v3와 v4는 동작이 완전히 동일하다** (맞대결 236-236 대칭 = 같은 수를 둔다).
차이는 지연이 3으로 커질 때만 나타나고, 거기서 v4가 조금 낫다 (AI 상대 100% vs 97%,
맞대결 57%). 즉 v4는 공짜 보험이다. 비용은 연산 6배(0.36ms → 2.3ms)뿐이고
둘 다 예산 120ms에 한참 못 미친다.

## v5 — 예측 수비로 v4를 잡는 원리

v4끼리의 미러전 랠리는 거의 항상 3타 교환(서버 스매시 → 수비 요격·반격 → 서버
재반격 → 득점)으로 끝나고, 승부는 "재반격이 꽂힐 때 수비자가 어디 있었는가"다.
v4의 수비는 코트 중앙 고정이라 여기서 진다.

v5는 공이 상대 쪽에 있을 때 상대의 접촉 시점을 추정하고, 가능한 수 7종
(파워히트 6 + 몸통 리턴)의 **낙하 지점**을 전부 시뮬레이션해서, 가장 받기
어려운 수까지의 부족 거리가 최소가 되는 위치(maximin)에 미리 선다.

측정으로 배운 것:
- 위협 커버리지를 "비행 중 요격"으로 재면 과대평가 (점프 선행 프레임 필요).
  믿을 건 낙하 지점 선점뿐 → 위협을 낙하 지점으로 요약해야 작동한다.
- 후보 위치는 접촉 전 도달 가능 범위로 제한해야 한다.
- 지상 상대에도 예측 전량 반영(TRUST=1)이 실측 지연(1)에서 최선.
  **지연 2에선 약한 상대에게 뒤집히는 트레이드오프가 있다** (21~25%) —
  지연 1은 실측값이라 감수했다. 대회 당일 지연이 달라 보이면 재검토.
- 공중에서 떠나가는 공 추적 중단(DEF_AIR)은 조합 시 순손해 → OFF.

성능 (오프라인, jitter=8, 24세트/칸): lag1 기준 vs AI 75% / v2 75% / v3 100% / v4 100%.
브라우저 실전: v5 vs v4 10-3 승.

## v6 — 커밋 겨냥 공격 (결과: v5와 동급)

v5까지의 공격 채점은 수비수를 "전지적"으로 가정했다(reach = 6*(t+headStart)).
v6는 상대 속도를 외삽한 타격 시점 위치 p-hat + 반응 지연 4프레임 모델로
채점한다 (reach = 6*max(0, t-4) + 32). 수비는 v5와 동일.

**"v5를 이긴다"는 목표는 실패했다.** 배운 것:

- **집안 맞대결은 신호가 없다.** v5-v6 랠리 로그를 까보면 특정 진영 조합에선
  33랠리 전부 "서브 넣은 쪽이 승리" — 세트 승부가 순수 서브 추첨이다.
  초기에 보고했던 "vs v5 73~95%"는 지터·시드 운이 만든 허수였다.
- **CONTACT_GAIN=15 실험은 회귀였다.** 특정 시드에서 좋아 보였지만 상대 풀
  종합 72.5%로 후퇴. 60으로 복원.
- **측정 프로토콜 교훈: 상대 풀 종합(지터 0+8, 시드 24개, 양 진영)이 아니면
  믿지 말 것.** 소표본 단일 조건 수치는 ±20%p씩 튄다.

최종 성적 (lag1, 지터 {0,8}, 상대당 48게임):

| | vs AI | vs v1 | vs v2 | vs v3 | vs v4 | vs v5 | 풀 평균 |
|---|---|---|---|---|---|---|---|
| v5 | 83% | 83% | 85% | 100% | 100% | — | 90.2% |
| v6(G=60) | 90% | 94% | 83% | 98% | 98% | 50% | 92.6% |

## v7 — 공중 낙하점 수정 + 시간 게이트 와리가리 (현재 주력)

두 가지가 들어갔고, 가치는 첫 번째가 대부분이다.

**1. 공중 낙하점 버그 수정 (진짜 개선).** v5·v6에 잠복하던 버그: 공중에서
이 공을 칠 수 없으면(contact < 0) 공의 "2프레임 뒤 위치"를 쫓는데, 빠른
반격은 나를 지나쳐 등 뒤에 떨어진다. 낙하점이 x=36인데 x=184까지 반대로
걸어가는 장면을 프레임 트레이스로 확인했다. 수정: 내 코트로 오는 공을
공중에서 못 치면 낙하점 옆 리시브 위치로 즉시 걷는다.
이 수정만으로 **v5·v6을 맞대결 60%로 이긴다** (동전던지기가 아닌 진짜 우위).

**2. 시간 게이트 와리가리 (비용 0의 보험).** 수비 대기 중 상대 접촉이
22프레임 이상 남았을 때만 maximin 위치 주변 ±20px를 Math.random 타이밍으로
왕복한다. 접촉이 다가오면 정확 수렴. 위치·속도를 외삽하는 상대의 조기
조준을 교란하고, 우리 수비엔 비용이 없다 (게이트 ON/OFF 성적 동일 실측).

**와리가리에서 배운 것 (중요)**: 상시 진동은 재앙이었다 (vs v5 0%).
집안 랠리는 20px 미만 칼끝 마진으로 갈리는데 진동 변위가 그걸 전부
실점으로 바꿨다. 슬랙 게이트(-worst 여유만큼 진동)도 실패 — 슬랙은
"낙하점 걷기" 모델의 여유인데 실제 수비는 점프 요격이라 허수였다.
**타격 순간의 위치가 전부인 이 게임에서, 마지막 순간까지 위치를 숨기는
것은 반드시 수비력을 지불한다.** 시간 게이트만이 무해했다.

최종 성적 (lag1, 지터 {0,8}, 상대당 48게임):

| | vs AI | vs v1 | vs v2 | vs v3 | vs v4 | vs v5 | vs v6 | 풀 평균 |
|---|---|---|---|---|---|---|---|---|
| v7 | 83% | 100% | 81% | 98% | 98% | **60%** | **60%** | 82.9% |

decide 실행시간: 지상공격 1.7ms / 수비 0.19ms (예산 120ms).
브라우저: 로드 정상, 타임아웃 0, v5와 접전 확인 (8:10 — 60% 승률이면 정상 범위).

브라우저 실전 시리즈 (빠른 속도, 진영 교대 8게임): v6 5승 3패 — 동급 확인.

→ **제출 권장: v7** — v5·v6과 풀 성적이 같으면서 유일하게 그 둘을
맞대결에서 이긴다(60%). lag2 이상 의심 환경이면 v3.

## 재검증 후 재기각 — 다단 운반 후 네트 앞 내리꽂기 (v8 실험)

"4회 접촉을 써서 몸통 리턴으로 공을 네트 앞까지 옮긴 뒤 급강하로 마무리"를
v8로 온전히 구현해 검증했다 (v4의 1홉 기각을 넘어: 하강 경로 통과 판정,
2홉 체이닝, 마무리 접촉 고정까지). **결론은 재기각.**

- **기하학적 반증**: "상대가 어느 위치에 서 있어도 최소 한 옵션은 못 받는"
  포크 조건(min over 수비위치 p, max over 슛 6종 margin > 0)을 전 코트
  스캔으로 계산하면, 성립 지점이 **x=242(네트기둥 경계+1px), y=110에서
  여유 +4px 단 한 점**이다. 이동 단위 18px/tick으로는 도달 불가능한 창이다.
- **물리적 이유**: 수비 도달 반경은 비행 프레임당 6px씩 자라는데, 한 접촉점의
  슛 스프레드가 그걸 못 이긴다. 드롭(3~6f)은 네트 근처(140~190)에 떨어져
  센터 수비(108) 커버 안이고, 플랫(19f)은 어디서든 따라잡힌다. 세팅 30~70
  프레임은 상대에게 200~400px의 공짜 재배치다.
- **실측**: 세팅 발동 랠리 승률 26~33% vs 직접 공격 60~62%. 5터치 자멸은
  드물었다 — 규칙이 아니라 마무리가 커버당하는 게 문제다.
- 대회 당일 스킬로 물리가 바뀌면(스매시 강화·창 확대) 포크 스캔을 다시
  돌려볼 가치는 있다. 판정 코드는 git 히스토리의 v8 forkMargin 참고.

**패치 사고 교훈**: v8 생성 스크립트의 교체 마커가 어긋나 함수가 두 벌
복제됐고, JS는 나중 선언이 이겨서 구식 코드가 조용히 새 코드를 덮어썼다.
"수정했는데 수치가 안 변하면" 원인 분석보다 **파일 구조 diff부터** 할 것.
